{
  "name": "actions-git-discord-webhook",
  "version": "1.0.0",
  "description": "This is a hard fork of actions-discord-git-webhook, with changes to the embed message.",
  "main": "src/app.js",
  "author": "Tim203",
  "license": "GPL-3.0",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.0.0",
    "discord.js": "^13.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1",
    "prettier": "^2.4.0"
  },
  "scripts": {
    "prepare": "ncc build src/app.js -o dist --source-map"
  },
  "standard-version": {
    "skip": {
      "changelog": true
    }
  }
}
